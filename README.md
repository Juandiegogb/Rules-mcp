# Instrucciones

```
  # Crear entorno virtual e instalar dependencias:
  uv sync

  # Crear .env con credenciales a una base de datos antigua (Proyecto PostgreSql)

  cp .env.example .env

  # Activar venv
  source .venv/bin/activate

  # Correr script
  python main.py

```
